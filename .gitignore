# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Empirical capture-driven validation test. Requires sample data in
# _sample_data/iptv-proxy/Provider*/ which lives in the iptv-proxy
# workspace, not the library repo. Skipped silently when sample data is
# absent, so it added no CI value but did show up in `go test` runs.
provider_validation_test.go
